export interface ResourceType { title: string, name: string };

export interface ResourceContentType { name: string, contents: string };

export interface NotesType { name: string, contents: string };

export interface Message { role: string, content: string };
